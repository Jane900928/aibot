# DeepSeek AI Chat - 环境变量配置

# Cloudflare Worker 部署后的 GraphQL 端点
# 将下面的 URL 替换为你的 Worker 部署地址
NEXT_PUBLIC_GRAPHQL_ENDPOINT=https://your-worker.your-subdomain.workers.dev/graphql

# 开发环境配置
NEXT_PUBLIC_APP_ENV=development
